import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nexora.tenantfrontend',
  appName: 'Nexora Tenant',
  webDir: 'dist'
};

export default config;
